# MAINT SUPER — environment config
# Copy to .env and fill in your values

DB_PATH=maint.db
BASE_URL=https://maint.whitwerx.net

# Brevo SMTP notifications (leave blank to disable)
NOTIFY_EMAIL_TO=
NOTIFY_EMAIL_FROM=
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFY_DAYS_AHEAD=7
